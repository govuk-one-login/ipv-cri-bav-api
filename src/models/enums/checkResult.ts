export enum CopCheckResult {
	FULL_MATCH = "FULL_MATCH",
	PARTIAL_MATCH = "PARTIAL_MATCH",
	NO_MATCH = "NO_MATCH",
}

export enum ExperianCheckResult {
	FULL_MATCH = "FULL_MATCH",
	NO_MATCH = "NO_MATCH",
}
